mixin textfield(input_type, title, placeholder)
  if title != ""
    label(for=input_type id="textfield_label") #{title}
  br
  input(class="textfield_input" type=input_type placeholder=placeholder id=input_type)
